<!DOCTYPE html>
<html lang="en">
  <head>
    <% include ../partials/head %>
  </head>
  <body>
    <header class="site-header">
      <% include ../partials/header %>
      <div class="media-search-header">
        <div class="container">
          <h1 class="">Recently Released in <%= country %></h1>
        </div>
      </div>
    </header>
    <main>
      <div class="container">
        <section class="section">

          <div class="u-clearfix">
            <% if (results) { %>

              <% results.forEach( media => { %>

                <%- include('../partials/media-thumbnail', {
                  media: media,
                  mediaType: "movie",
                }) %>

                <% }) %>

                <% } %>
          </div>
        </section>
      </div>
    </main>
    <footer>
      <% include ../partials/footer %>
    </footer>
  </body>
</html>
